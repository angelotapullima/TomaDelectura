import 'package:flutter/material.dart';

class DetalleBusquedaclientePage extends StatelessWidget {
  const DetalleBusquedaclientePage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
          body: Center(
            child: Container(
        child: Text("Detalle del registro"),
      ),
          ),
    );
  }
}