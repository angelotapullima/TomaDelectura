import 'package:flutter/material.dart';

class DetalleBusquedaMedidorPage extends StatelessWidget {
  const DetalleBusquedaMedidorPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
          body: Center(
            child: Container(
        child: Text("Detalle del registro"),
      ),
          ),
    );
  }
}